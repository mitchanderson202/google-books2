name: Google Books Tests

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
       - name: Run component tests
        run: npx cypress run-ct
        with:
          start: npm start
